# 本地验收记录

版本：2026-09-07。本记录形成于本地验收阶段；实际发布状态以 GitHub Pages 部署记录为准。

## 已验证

- Vinext 生产构建成功，6 项自动测试通过：HTML 三副本一致、主题与图片数量、离线资源、页内锚点、脚本语法、真实应用入口渲染。
- 16 个主题、39 个可展开说明、9 个关系 / 流程图、2 张嵌入式实拍图、18 个分组勾选项。
- Chromium 浏览器检查：320、360、390、430、768、1024、1440 px；普通字号与根字号放大 200%，展开所有说明，未发现整页横向溢出。
- 搜索“苹果果肉”可找到具体内容，点击结果后关闭搜索并展开对应说明；目录跳转正常。
- 浅色 / 深色切换正常。按渲染后的前景色与祖先不透明背景计算，每个主题检查了 619 个可见含文字节点：浅色最低约 4.93:1，深色最低约 5.59:1；均通过各自字号所需阈值。此检查不等于完整 WCAG 无障碍认证。
- 勾选刷新后保留。模拟从 2031-03-30（周日）到 2031-03-31（周一），每日 / 每周勾选归入新周期，长期准备项仍保留。
- 打印事件会展开全部说明，打印结束后恢复原先展开状态。
- 下载按钮实际生成 HTML；浏览器断网后通过 file:// 打开下载文件，16 个章节与两张照片正常，搜索“换粮”返回结果。
- 禁用 localStorage 后，有明确提示，阅读、搜索与临时勾选仍可使用。
- 图片源、作者及 CC0 许可已核验；使用缩小版本，不裁掉绿颊长尾。

## 发现并修复

- 章节跳转时，根节点 scroll-padding 与章节 scroll-margin 重复累加固定顶栏高度，已改为根节点统一负责。
- 页面图标加入 HTML 内嵌，减少离线外部依赖和在线 favicon 404。
- 原始模板残留的 loading skeleton 测试已更新为实际手册入口测试。

## 尚需真机确认

- iOS / 安卓实际微信内核、软键盘、底部安全区与系统字号设置。
- 微信版本对 Blob 下载和“在浏览器打开”的限制；不同手机文件预览器对 JavaScript / localStorage 的支持。
- 实体打印机与不同系统 PDF 导出的分页细节。
- 本轮是护理事实重点核对和编辑，不是全部内容获得鸟类兽医逐条签字审校。

测试截图与临时验收脚本位于被忽略的 output/playwright 目录，不纳入网站发布。
